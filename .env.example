# Example environment variables
# Replace placeholders with real values before using.

# For production use a Postgres connection string (Production)
# Format: postgres://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgres://user:password@db-host:5432/dbname"

# NEXTAUTH settings
NEXTAUTH_URL="https://us-omega-eosin.vercel.app"
NEXTAUTH_SECRET="<generate-a-32-byte-hex-secret>"

# Development convenience (optional):
# You can use SQLite locally by setting DATABASE_URL=file:./prisma/dev.db
# NODE_ENV=development
# FEATURE_ENABLE_GPT5_PREVIEW=true

# Note: In CI you should provide a Postgres test DB (service container or hosted)
