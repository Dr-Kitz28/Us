# Production environment variables (example)
# Set these in Vercel (Project Settings → Environment Variables) or your host.

# PostgreSQL connection string
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# Redis connection URL (for ioredis / general cache)
REDIS_URL=redis://:PASSWORD@HOST:PORT

# Next.js runtime flags (optional)
NEXT_PUBLIC_APP_ENV=production

# Auth / third-party keys (example placeholders)
NEXTAUTH_URL=https://your-production-domain.com
NEXTAUTH_SECRET=replace-with-secure-secret

# Optional observability
SENTRY_DSN=

# NOTE: Do NOT commit real secrets. Use your platform's secret storage.
